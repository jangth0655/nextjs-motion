import Layout from "@components/layout";
import { NextPage } from "next";

const Chat: NextPage = () => {
  // login user Data - me
  // receive click -> post id
  // if(!login user) => push("/")
  return <Layout goBack={true}></Layout>;
};

export default Chat;
